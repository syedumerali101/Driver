import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    height:70,width: 160, backgroundColor: '#276df0', alignItems: 'center', justifyContent: 'center', borderColor: '#3073f1', borderWidth: 1
  },
  
  labelContainer:{
    color:'white', fontSize: 16, fontWeight: 'bold'
  }
});
